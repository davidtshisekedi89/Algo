function message(element){
    // alert('do not touch');
    element.innerText = 'RUN';
    console.log(element)
}


function hidenBtn(element){
    // element.remove();
    element.innerText = "Coollll!";
    console.log(element);
}